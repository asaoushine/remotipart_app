<%= form_for @remotipart_pic, :html => { :class => "form-horizontal remotipart_pic" } do |f| %>

    <% if @remotipart_pic.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@remotipart_pic.errors.count, "error") %> prohibited this remotipart_pic from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @remotipart_pic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%= error_span(@remotipart_pic[:title]) %>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'form-control' %>
    </div>
    <%= error_span(@remotipart_pic[:description]) %>
  </div>
  <div class="control-group">
    <%= f.label :topimage, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :topimage, :class => 'form-control' %>
    </div>
    <%= error_span(@remotipart_pic[:topimage]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            remotipart_pics_path, :class => 'btn btn-default' %>

<% end %>
